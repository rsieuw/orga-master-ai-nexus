/**
 * @fileoverview Re-exports Supabase related types from the integration directory.
 * This allows for a cleaner import path for these types throughout the application.
 */
export { type Json, type Database } from '@/integrations/supabase/types.ts';
